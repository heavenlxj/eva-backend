apiVersion: v1
kind: Service
metadata:
  name: {{ include "kido.fullname" . }}
  labels:
    {{- include "kido.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kido.selectorLabels" . | nindent 4 }}
